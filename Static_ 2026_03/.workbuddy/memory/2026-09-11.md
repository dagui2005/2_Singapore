# 2026-09-11

## Step 0 数据审计：增强并跑通 scripts/od/audit_all.py

- 位置：`D:/Luan/2026-05/2_Singapore/scripts/od/audit_all.py`
- 输出：`D:/Luan/2026-05/2_Singapore/reports/od_audit/`（audit_summary.json / audit_zone.csv / audit_census.csv / audit_acra.csv / audit_network.csv / audit_trafficflow.csv + 2 个附加）
- 基线版备份：`scripts/od/_backup/audit_all_baseline.py`
- 运行：`"C:/Users/LQP/miniconda3/python.exe" scripts/od/audit_all.py`（67s，conda python 有 pandas 3.0.1 / geopandas 1.1.3 / numpy 2.3.2 / pyshp / shapely）
- 首轮基线结果：PASS 47 / WARN 4 / FAIL 1 → 修正后：**PASS 64 / WARN 5 / FAIL 0**

### 四项关键审计结论（用户最关心）
1. **Census 空间一致性 ✅**
   - residence(55) / workplace(44) / travel(30)，关系为 44⊂55、30⊂55，**三套共同 PA = 29 → 建模核心区**
   - ⚠️ 唯一异常：**River Valley** 出现在 travel(30) 但不在 workplace(44)，需人工确认
   - Table 118 总量 2,177,456 vs Workplace T4 总量 2,208,358，**差 -30,902（1.4%）**，需在建模时对齐口径
2. **ACRA → Subzone 落点 ⚠️ 83.5%**（未达 90% 目标）
   - 桥接表 = URANoof(83,541点) + hdb.csv(12,441) + poi.csv(2,797) + OSM建筑/点(31,195) → **104,420 个唯一邮编**
   - ACRA 总 1,049,322 行；有效邮编 97.7%；匹配 858,730 行（全部 81.8% / 有效邮编 83.5%）
   - 未匹配 = 18,994 个桥接表完全缺失的邮编（长尾，前缀分散 20/18/04/38/41…）→ **本地无法补齐，需 OneMap geocoding**
3. **LTA ↔ OSM 链路 ✅**：几何命中 1278/1278=100%（25m 内）；**名称命中 1156/1278=90.5%**；未匹配 0
4. **MATSim network ⚠️**：`network.xml.gz` 尚未生成（属开发步骤，非数据缺失）

### 关键技术发现（可复用）
- **`Changi- Total` 陷阱**：SingStat 导出中 Changi 的 PA 合计行连字符前**无空格**，用 `.endswith(" - Total")` 会漏判 → 分类正则须用 `^(.+?)\s*-\s*Total$`。修正后 55 PA + 332 Subzone 全部对上
- **osm-lines.dbf 编码坑**：`.cpg` 声明 UTF-8，但整文件 UTF-8 解码失败（"unexpected end of data"），因存在非法字节。解决：`gpd.read_file(path, columns=['name','highway'], encoding='latin-1')`，2.1s 读出 275,996 条，按列读还能避开 604MB 的 other_tags
- **OSM 自带地址**：`osm-polygon`(155,074 建筑) 与 `osm-points`(110,907) 的 `other_tags` 含 `addr:postcode"=>"NNNNNN"`，格式统一；用 pyshp latin-1 流式提取 + 点空间连接 Subzone，是 ACRA 落点的关键补足源
- **Table 118 是三维交叉表**：列按 Residence Region × Mode 分组，首数据列就是 "Total"(2,177,456)，**不能对整行求和**（会重复计数到 8.7M）
- **ACRA 邮编是整数存储**：前导 0 丢失（如 '018956'→18956），必须 `zfill(6)` 还原；有效新加坡邮编 = 6 位且前两位 01–82（无效仅占 2.1%）

## Step 1 空间单元冻结：build_zone_dictionary.py

- 用户已写好 `scripts/od/build_zone_dictionary.py`，我运行验证：**332 Subzone / 55 PA / 5 Region，PASS**
- 输出 `reports/od_zone/`：zone_dictionary.csv（332）、planning_area_dictionary.csv（55）、zone_dictionary_validation.json
- 空间体系**正式冻结**：332 Subzone（最终 OD 单元）→ 55 PA（统计控制）→ 5 Region（Table 118 粗约束）
- 统一 CRS = **EPSG:3414 / SVY21**（面积、质心、后续距离）；zone_id 按 SUBZONE_C 排序后从 1 编号
- 我做的修正：脚本 docstring 首行改 `r"""`（消除 `\L` 的 SyntaxWarning）
- QC：Subzone 面积合计 784.8 km² vs PA 784.9 km²（差 0.02%）；无空值；最大 Subzone 是 Western Water Catchment 68.55 km²、North-Eastern Islands 67.25 km²、Changi Airport 35.67 km²（均为真实大地块）；Region 分布 CR135/ER30/NER48/NR41/WR78 个 Subzone

## Step 2 居住端产出：build_production.py（新建）

- 位置 `scripts/od/build_production.py`；输出 `reports/od_production/`：production.csv（332）、pa_control_totals.csv（31 控制组）、zone_mode_shares.csv（332×11 方式）、production_validation.json
- **方法（非"总人口×比例"）**：分配键 = Subzone 15+ 人口（分男女，Table 88）；控制量 = PA 级就业居民（分男女，Table 97）；组内归一化到控制总量
  - 控制组 g = 30 个 Census 地理分布表指名 PA，或 "Others"（其余 25 个物理 PA 合并）
  - `Σ_{i∈g} P_i^work ≡ Employed_g`（逐组守恒，实测最大偏差 2e-6）
  - `P_i^car = P_i^work × (Car Only_g / Employed_g)`，分母刻意用 Employed（非 Table104 合计）以保证 `Σ P^car ≡ 459,796`
- 结果：**PASS**；ΣP^work = 2,208,356（锚点 2,208,358，差 −2）、ΣP^car = 459,794（锚点 459,796，差 −2）
- 98 个零产出 zone 全部 pop_15plus==0（工业/水务/岛屿/CBD），无异常
- 汽车分担符合现实：Tanglin 49.6% > Bukit Timah 48.4% > Marine Parade 36.2% …… Woodlands 14.7% / Yishun 15.9% / Downtown Core 12.9%

### 口径关键发现（重要）
- **居住端就业居民总量 = 工作端 T4 总量 = 2,208,358**（Table 97 全国行 vs Table 111）→ Step 2 的硬锚点
- **30,902 的差额 ≠ WFH**：Table 104/118 总量 2,177,456，其中 `No Transport Required = 212,907` 是**含在**总量内的；2,208,358 − 2,177,456 = 30,902 来源未在本地数据说明 → 按用户要求作为**显式口径差异参数**记录，不做人为修正
- **Table 88 按最接近 10 取整**：全表 Total 行 15+ = 3,456,030，但 Subzone 聚合 = 3,456,820、PA 聚合 = 3,456,140，三级互不闭合（非解析错误）；Downtown Core 小值区尤其明显（PA-Total 2,940 vs Subzone 聚合 2,770）
- **Census 存在 `Employed_total ≠ male + female` 的 ±1 取整** → 所以需要组内归一化，否则 11 个组各差 1 人
- 就业居民**只有 PA 级（30 PA + Others），没有 Subzone 级** —— 这是 Step 2 必须下分的原因

### 控制组映射（55 物理 PA → 30 PA + Others）
- 30 个指名 PA 与 55 PA **完全对应无遗漏**
- 归入 "Others" 的 25 个 PA：Boon Lay / Central Water Catchment / Changi / Changi Bay / Lim Chu Kang / Mandai / Marina East / Marina South / Museum / Newton / North-Eastern Islands / Orchard / Paya Lebar / Pioneer / Rochor / Seletar / Simpang / Singapore River / Southern Islands / Straits View / Sungei Kadut / Tengah / Tuas / Western Islands / Western Water Catchment
- 后果：Orchard/Newton/Rochor 等有居民的小 PA 就业量被合并到 Others 块，分辨率下降 —— 建模时需注意

### 环境
- 用 `C:/Users/LQP/miniconda3/python.exe`（有 geopandas/pyshp/shapely/numpy）；managed python 缺这些
- bash 需先 `export PATH=...:/c/Users/LQP/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:$PATH`，否则 ls/dirname 找不到

## Step 3 工作端吸引量：build_attraction.py（PASS，运行 1m53s）

- 位置 `scripts/od/build_attraction.py`；输出 `reports/od_attraction/`：pa_workplace_control.csv、acra_subzone_activity.csv、subzone_landuse_weights.csv、attraction.csv(332)、attraction_pa_validation.csv(55)、attraction_validation.json，另加我写的 **STEP3_REPORT.md**
- 方法：Census Workplace PA 就业控制总量 → ACRA(0.65)+LandUse(0.35) 空间权重 → Subzone 归一化，强制 `Σ_{z∈PA} A_z ≡ E_PA`
- 结果：**PASS**。物理 44 PA 控制 = 1,935,235；3 特殊节点 = 273,124（Other 24,736 / No Fixed 177,588 / WFH 70,800）；phys+spec = 2,208,359 vs 全国 2,208,358 → **残差 +1**（源表取整，记为口径参数）
- 守恒：max abs err 1.46e-11，max rel err 1.89e-16（机器精度）
- ACRA：1,049,322 行 / 有效邮编 1,046,847 / 定位 858,741 / 定位率 **82.03%**（分母为 6 位有效邮编，故略低于 Step 0 的 83.5%）
- 25 个零 attraction Subzone 全部落在 11 个"仅居住端"PA（TENGAH/RIVER VALLEY/SIMPANG/MANDAI/…）；307 个正值

### 我修复的两个真实 bug（非权重值问题）
1. **T4 解析**：`parse_census_pa_rows` 原正则只认 `X - Total` 后缀，但 Census T4 实际是**裸 PA 名**（shape 48×34，首列 `Planning Area of Workplace`，Total 列名就是 `Total`），导致匹配为空 → KeyError 崩溃。改为兼容"裸名 / 带后缀"两种形态
2. **LandUse 权重未生效**：`gpd.overlay` 未把 `lu_weight` 列带入，`weighted_intersection` 恒走 else 分支退化成"裸面积"，`LANDUSE_WEIGHT` 字典被完全忽略。已把 `lu_weight` 加入 overlay 选择的列

### 合理性正信号
- Downtown Core 284,004 居首；Top Subzone 全在 CBD：CECIL 49,394 / CITY HALL 40,526 / RAFFLES PLACE 37,410
- 区域份额 Central 53.6% / West 17.7% / East 12.7% / NE 8.4% / North 7.6%
- `corr(acra_activity_weight, attraction)=0.766`，`corr(landuse_activity_weight, attraction)=0.135` → ACRA 主导，符合设计

### Step 4 前建议处理（已写入 STEP3_REPORT.md）
1. **面积型 LandUse 在大面积低强度用地上被放大**：JURONG ISLAND AND BUKOM 仅 5 家企业却吸收 Western Islands PA 的 **90.7%**；MURAI 78.9%（198 家）、CHANGI AIRPORT 76.4%（583 家）→ 建议引入 **GPR 建筑面积权重**（`area × GPR`）
2. **LU_DESC 权重覆盖不足**：`LANDUSE_WEIGHT` 仅命中 15/33 类，18 类共约 6,675 个多边形落默认 0.2。其中 **PORT/AIRPORT→0.2 偏低**、**WATERBODY→0.2 偏高（应为 0）**、COMMERCIAL & RESIDENTIAL / */INSTITUTION 偏低
3. **13 个 Subzone 无 ACRA 但 attraction>0**（纯 landuse 兜底）：LORONG HALUS / GUL BASIN / SEMAKAU / SUDONG / AIRPORT ROAD / MARINA EAST(MP) 等，多为填埋场/离岛/机场附属地，属假信号
4. 输出字段与规划口径差异：脚本写 `subzone_landuse_weights.csv`（规划名为 subzone_activity_weights.csv）；attraction.csv 暂无分类用地面积列与 poi_activity_weight（poi.csv 已定义未接入）
- 土地利用层实际字段：`OBJECTID, LU_DESC, LU_TEXT, GPR, WHI_Q_MX, GPR_B_MN, INC_CRC, FMEL_UPD_D, SHAPE.AREA, SHAPE.LEN, geometry`（CRS EPSG:4326，113,212 个要素，33 类 LU_DESC）
- 边界层字段：`SUBZONE_NO, SUBZONE_N, SUBZONE_C, CA_IND, PLN_AREA_N, PLN_AREA_C, REGION_N, REGION_C, ...`
- 44 个工作地 PA 与 zone 字典 **全部精确匹配**；11 个仅居住端 PA：CENTRAL WATER CATCHMENT / CHANGI BAY / LIM CHU KANG / MANDAI / MARINA EAST / MARINA SOUTH / NORTH-EASTERN ISLANDS / RIVER VALLEY / SIMPANG / STRAITS VIEW / TENGAH

## Step 3B 工作端吸引量 v2：build_attraction_v2.py（PASS，1m58s）

- Step 3A 基线已快照为 `reports/od_attraction_v1/`（原 `od_attraction/` 保留未覆盖）
- 输出 `reports/od_attraction_v2/`：attraction_v2.csv(332)、attraction_v2_pa_validation.csv、attraction_v2_suspicious.csv、attraction_v2_validation.json、special_workplace_destinations.csv，另加我新建的 **attraction_diagnostics.csv / attraction_v1_v2_comparison.csv / attraction_diagnostics.json / landuse_class_mapping.csv / STEP3B_COMPARISON.md**，脚本 `scripts/od/attraction_diagnostics.py`
- 权重（未改动）：ACRA 0.55 / GFA 0.30 / LandUse 0.10 / POI 0.05
- 结果：**PASS**。控制总量 1,935,235，attraction 合计 1,935,235，max PA abs err 7.28e-12 / rel 1.98e-16；零 attraction 仍 25 个；ACRA=0 但 A>0 共 17 个（v1 为 13）

### 我修复的 3 个运行级 bug（读表/合并问题）
1. **OSM 编码坑复现**：`gpd.read_file("07_RoadNetwork/osm-polygon.shp", encoding="latin-1")` 中 pyogrio **忽略 encoding**，`other_tags` 非法字节触发 `UnicodeDecodeError: byte 0xe5`。修复：改读 **`osm-polygon_expanded.shp`** 的 ASCII 列 `building:l`(=building:levels) / `height` / `building`（GDAL 只取 ASCII 列不碰 other_tags，1.1s 读入 155,074 条）。**规则：本数据集所有 `osm-*.dbf` 读取一律用 `*_expanded.shp` + 只取 ASCII 列，或 pyshp `encoding="latin-1"`**
2. `acra_zone()` 返回表含 `planning_area_norm` → 按 `subzone_code` merge 回 z 时列名冲突被拆成 `_x/_y` → `KeyError`。修复：只返回 `subzone_code` + 统计量
3. 同上，返回表还含 `zone_id` → 后续 `groupby.agg(zone_id=...)` `KeyError`。一并移除。**规则：聚合表 merge 回主表时，只保留连接键，绝不带主表已有的列**

### 决定性发现（本轮核心）
- **建筑层数覆盖率仅 20.6%**：155,074 栋中 `building:levels` 有值 30,866(19.9%)、`height` 回退 1,140(0.7%)、缺省 1 层 123,068(79.4%) → **GFA ≈ footprint，退化成又一次面积权重**。组件相关性：ACRA 0.761 > **POI 0.551** > GFA 0.184 > LandUse 0.171
- **v2 未解决 v1 被点名的错配**：JURONG ISLAND AND BUKOM 90.72%→**91.06%**（更差）、MURAI 78.87%→**79.66%**（更差）、UPPER THOMSON 79.86%→62.99%（改善 17pp）、CHANGI AIRPORT 76.42%→68.24%（改善 8pp）
- 整体重分配量 `Σ|A_v2−A_v1| = 497,969`（占控制总量 **25.7%**）；Spearman(share_v1,share_v2)=0.815；最大变动 ANSON DOWNTOWN CORE +16,873 / MARINA CENTRE −9,477
- **主因：组内 min-max 归一化无量纲**（`transform(norm)` 把每 PA 最大值拉到 1.0，绝对量级被抹平 → 5 家企业的 zone 与 5,000 家企业的 zone 同为 1.0，且 0.05 下限让零活动 zone 仍分份额）

### LU 类别映射 bug（已修复并外置）
- v2 的 `LU_FACTOR` 只匹配 **23/33** 类。真实 `LU_DESC` 用 `" - "`（带空格）和 `"PORT / AIRPORT"`，脚本写成 `B1-WHITE`/`B2-WHITE`/`BUSINESS PARK-WHITE`/`PORT`/`AIRPORT`/`BEACH` → **全错**；`HOTEL`(285 多边形) 被漏写（v1 原有 1.5）
- 后果：**PORT / AIRPORT 仍落默认 0.20**，"airport/port 应高于空地"的意图完全未生效
- 已交付 `reports/od_attraction_v2/landuse_class_mapping.csv`：33/33 覆盖，列 = `LU_DESC, employment_factor, building_factor, allow_employment, calibrated, note`；5 类标 `calibrated=N`（UTILITY / COMMERCIAL / INSTITUTION / HOTEL / MASS RAPID TRANSIT / LIGHT RAPID TRANSIT）
- 土地利用层实际 33 类 `LU_DESC` 全清单已记录（见上表），真实值含 `BEACH AREA`、`PORT / AIRPORT`、`BUSINESS 1 - WHITE`、`BUSINESS PARK - WHITE`、`COMMERCIAL / INSTITUTION`

### 建议 Step 3B.1（只动结构，不动 4 个组件权重）
A. 归一化改为保序保量级（`log1p` 后按 PA 求和），去掉 0.05 下限
B. 建筑层数用 `building`/`residentia` 类型给典型层数先验（HDB≈12、公寓≈10、排屋≈2、工业≈3），再叠加实测 levels
C. Lu 映射用 `landuse_class_mapping.csv` 驱动
D. POI 相关性 0.551 高于其权重 0.05，留待 TrafficFlow 校准阶段上调
- 补充事实：Jurong Island 现实为石化集群、确有大量在岗人员，而 ACRA 仅登记 5 家实体 → **ACRA 登记数 ≠ 作业强度**，"Jurong Island 占比高"未必错，但当前是 min-max 凑出来的
- 用户明确：暂不冻结 attraction_v2.csv；未得到指示前不擅自改 0.55/0.30/0.10/0.05

## Step 3B.1 工作端吸引量 v2.1：build_attraction_v21.py（PASS，约 1m18s）

- 新脚本 `scripts/od/build_attraction_v21.py`（**未改动 v2 脚本**，保证 v1/v2 可复现）；诊断脚本 `scripts/od/attraction_diagnostics_v21.py`
- 输出 `reports/od_attraction_v21/`（不覆盖 `od_attraction/`=v1、`od_attraction_v2/`=v2）：attraction_v21.csv、attraction_v21_pa_validation.csv、attraction_v21_suspicious.csv、attraction_v21_validation.json、attraction_v1_v2_v21_comparison.csv、attraction_v21_diagnostics.{csv,json}、attraction_v21_buildingfilter_impact.csv、pa_employment_concentration.csv、**building_level_prior.csv**、special_workplace_destinations.csv、**STEP3B1_COMPARISON.md**
- 用户指定三项改动（A/B/C），权重 0.55/0.30/0.10/0.05 **保持不变**：
  - **A 归一化**：PA 内 min-max(+0.05 floor) → **`log1p` 后 PA 求和归一化**（`x'=log(1+x)`，`s=x'/Σx'`），无 min-max、无 0.05 floor
  - **B 建筑层数**：缺失不再一律按 1 层，改按**建筑类型先验**；先验由"有实测层数的同类型建筑中位数"推导（`n_explicit>=20` 采信，否则回退全局中位数 3.0），写入 `building_level_prior.csv`（103 类：31 observed_median / 71 fallback / 1 default），**可人工标定**
  - **C LandUse**：改用 `reports/od_attraction_v2/landuse_class_mapping.csv` 的 `employment_factor`，33/33 全覆盖
- 结果：**PASS**。max PA rel err **1.89e-16**；attraction 合计 = 控制总量 1,935,235；零 attraction 仍 25 个

### 本步新发现的真实 bug（v2/v2.1 前缀版都有）→ 已修
- **osm-polygon 图层含 29,433 个无 `building` 标签的大面多边形，总面积 5,961 km²**（最大单个 **652.87 km²** 的边界/海域面）。原逻辑对全量 155,074 个多边形求面积 → 全岛建筑 footprint 从真实 **93.7 km² 虚增到 6,055 km²（约 64×）**，GFA 虚增到 31.1 亿 m²
- 修复两条：① 只保留 `building` 标签非空的**真建筑 125,632 个**（排除 29,433 无标签 + 9 个 `building=no`）；② 空间归属由 `intersects`（跨区重复计数）改为 **代表点 `representative_point()` 质心 `within` 归属唯一 Subzone**（8 个未归属）
- **规则：`07_RoadNetwork/osm-polygon_expanded.shp` 是「全要素多边形」图层，不是建筑专用图层；任何"建筑"用途必须先按 `building` 标签非空过滤**
- 修复后核验（全部合理）：Σfootprint **93.7 km²**（覆盖率 12.8%）、ΣGFA **4.90 亿 m²**、ΣLandUse 784.8 km²（与 Step 1 一致）、建筑覆盖率 11.9%、单栋 footprint 中位数 **223 m²**、mean levels **4.44**（v2 约 1.x）、实测层数覆盖 25.1%
- 该 bug 对 shares 的影响：Spearman(前缀, 修复后)=**0.975** → 主要污染量级而非排序；**A（归一化）才是份额变化的主因**

### 核心指标 v1 → v2 → v2.1
- 单区占 PA >50%：9 → 7 → **3**；>60%：7 → 6 → **2**；最大单区占比：95.5% → 95.5% → **81.9%**；PA 平均 top-1 占比：32.6% → 29.3% → **19.4%**
- 零 attraction 25 / 25 / 25；ACRA=0&A>0 17 / 17 / 17
- Spearman(share)：v1_v2 0.853、v2_v21 **0.773**、v1_v21 0.719；`Σ|ΔA|`：v2_vs_v1 497,969、**v21_vs_v2 759,869**
- 被点名异常区：JURONG ISLAND 90.7%→91.1%→**76.8%**；MURAI 78.9%→79.7%→**54.3%**；CHANGI AIRPORT 76.4%→68.2%→**42.2%**；UPPER THOMSON 79.9%→63.0%→**36.5%**；DHOBY GHAUT 62.9%→61.0%→**38.9%**
- 组件相关性（v2→v2.1）：ACRA 0.761→0.622、**GFA 0.184→0.303**、LandUse 0.171→0.193、POI 0.551→0.462 → 修复建筑图层后 GFA 终于携带真实信息

### 统计口径修正（重要）
- "单区占 PA >50%"**必须只在「有就业控制量的 PA」内统计**。11 个仅居住端 PA 无就业，其份额无意义；若一并计入会得到无意义的 11/10（诊断口径为 3/2）。已在主脚本加 `workplace_employment > 0` 约束

### 残留问题（本轮未动，交用户决策）
1. **17 个 ACRA=0 但 A>0** 中，约 8 个是**非就业承载用地**：LORONG HALUS（填埋场，GFA=0 纯 LandUse 兜底）、BAHAR（实弹射击区）、SEMAKAU / SUDONG / SOUTHERN GROUP / CONEY ISLAND / PULAU PUNGGOL TIMOR（离岛）、MARINA EAST (MP)（填海未开发）→ 建议 3B.2 引入 `allow_employment` 硬约束门控
   （另 6 个如 SHIPYARD / GUL BASIN / TENGEH / SAFTI / PLAB / AIRPORT ROAD 是**真有作业活动但无注册企业**，ACRA=0 合理，应保留）
2. **3 个 >50% zone 均可解释**：SENTOSA 81.9%（Southern Islands PA 仅 2 个 zone）、JURONG ISLAND 76.8%（Western Islands PA 3 区中唯一就业载体）、MURAI 54.3% → **非权重凑出来的**

### 建筑类型先验（可复用，来自实测中位数）
- residential **12**、apartments **11**、office **7**、dormitory 7、hospital 6、`building=yes` **3**、semidetached_house 3、industrial **4**、school 4、church 4、hotel 3、warehouse 3、commercial **2**、retail 2、house 2、terrace 2、detached 2、bungalow 3、garage/parking/bridge 5、train_station/public 1、roof 1；**全局中位数 3**、实测覆盖 20.6%

## Step 4 路网与阻抗矩阵：build_impedance.py（PASS，1m39s）

- 用户提供的脚本 `scripts/od/build_impedance.py`（原版依赖 networkx）→ 我重写为 scipy 版；输出 `reports/od_impedance/`：network_links.csv(706,554 有向边)、network_audit.csv/.json、zone_network_snap.csv(332)、**impedance_matrix.parquet/.csv(110,224 行 × 7 列)**、impedance_validation.json，另加 **STEP4_REPORT.md**
- 路网：**429,032 节点 / 706,554 有向边**；数据源 `07_RoadNetwork/osm-lines_expanded.shp`（275,996 要素 → 过滤出机动车道 168,580 条 / 10,003.5 km 无向）
- 结果：**reachable_rate = 1.000（0 个不可达）**；travel_time median 14.88 min / max 64.07 min；distance median 15.22 km / max 70.65 km；隐含速度 median 61.0 km/h（free-flow）；有向非对称比例 99.998%（|c_ij−c_ji| 中位仅 28 s）

### 我做的 3 处修复
1. **networkx 未安装**（conda 环境无）→ 改用 `scipy.sparse.csgraph.dijkstra`（C 实现，已装 1.17.1），输出 schema 不变。**规则：本项目最短路一律用 scipy csgraph，不要引入 networkx**
2. **oneway 分支脆弱**：原用 `edges[-1]` 回写判断，`-1` 方向有 IndexError 风险 → 改为显式三分支（yes→(u,v)、-1→(v,u)、其他→双向）
3. **吸附应限定在最大强连通分量（giant SCC）**：原吸附到弱连通分量内最近节点，但**弱连通≠有向可达**。实测 2 个 Zone 恰好吸附到单向死胡同节点 → **661 个不可达 OD**：
   - zone 141 TANJONG RHU → node 170853（indeg=1, **outdeg=0**，只进不出）→ 331 个不可达
   - zone 204 GHIM MOH → node 356100（**indeg=0**, outdeg=1，只出不进）→ 331 个不可达
   - 全网络仅 288 节点无入边、277 无出边（0.07%），却正好被这 2 个 Zone 命中
   - 修正后：giant SCC 占 98.22%；仅 **2 个 Zone** 改吸附节点，变化 +1.46 m / +2.29 m（**无一超 20 m**），中位数不变 → 修正是外科式的

### 关键数据事实
- **`oneway=yes` 占 46% 是真实的**：`oneway`/`maxspeed` 两列与原始 `other_tags` 逐条比对 **168,580 条 0 处不符**。新加坡大量干道为"上下行分离的单向车道"成对建图（primary 97.9% oneway、secondary 96.3%、motorway 90.4%）
- **`service` 道 maxspeed 覆盖率仅 3.6%**：89,906 条 service（占要素 53% / 长度 51.6%）中 96.4% 用默认 20 km/h —— 当前最大速度不确定性来源
- **5 个"无机动车道路接入"Zone**（已打 `no_road_access=True`）：NORTH-EASTERN ISLANDS 4.20 km、SOUTHERN GROUP 1.78 km、PULAU SELETAR 1.75 km、SEMAKAU 6.19 km、SUDONG 6.34 km（离岛，吸附到对岸）。其 **3,295 个 OD 对（3.0%）行程时间为几何外推伪值**，Step 5 需分离处理
- **本阻抗是 free-flow（无拥堵）**，非真正 AM peak；拥堵反馈留给后续 TrafficFlow 校准（Zone 07：75,899 条 / 07:00–10:00 有 49,754 条），λ 也应在那时标定
- 极值合理性：CHANGI BAY ↔ TUAS VIEW EXTENSION 64.07 min / 70.3 km（横穿全岛）；CBD 内部 BOAT QUAY→CHINA SQUARE 0.40 min / 0.33 km

### Step 5 输入已全部就绪
- `P_i` ← `reports/od_production/production.csv`（Step 2，Σ=2,208,358）
- `A_j` ← `reports/od_attraction_v21/attraction_v21.csv`（Step 3B.1，逐 PA 守恒 1.89e-16）
- `C_ij` ← `reports/od_impedance/impedance_matrix.parquet`（**110,224/110,224 可达**）
- 下一步：双约束 Gravity `T_ij=α_i β_j P_i A_j e^(−λ C_ij)`，行/列和分别守恒于 P_i / A_j

## Step 5 双约束 Gravity Prior OD：build_prior_od.py（PASS，约 2s）

- 用户提供 `scripts/od/build_prior_od.py`；输出 `reports/od_prior/`：physical_production.csv、special_destination_mass.csv、prior_od_accounting.json、prior_od_summary.csv、prior_od_diagnostics.csv、`prior_od_lambda_{0p050,0p075,0p100,0p125,0p150}.parquet/.csv`、prior_od_validation_lambda_*.json、network_access_mass.json，另加 **STEP5_REPORT.md**
- 口径：**`P_i^phys = r·P_i`，`r = A_total/P_total = 1,935,235/2,208,356 = 0.876324`**；再做双约束 IPF。**特殊工作地 273,124 不进入 332×332**（No Fixed 177,588 / WFH 70,800 / Other PA 24,736）
- 结果：**PASS**。`od_total ≡ 1,935,235`；行/列守恒 max abs err **6.6e-9**（≤ IPF_TOL 1e-8），5 个 λ 全部 `ipf_converged=True`（迭代 10/13/16/20/25 次）
- 平均通勤：λ 0.05→0.15 为 **13.74 → 13.07 → 12.43 → 11.82 → 11.26 min**（单调递减 ✓）

### 我修复的 1 处接线 bug
- `get_special_workplace_totals()` 找 `pa_workplace_control_v21.csv`，但 v21 实际产出 **`special_workplace_destinations.csv`**（列 `planning_area_norm/is_special_destination/workplace_employment` 完全匹配）→ 不接线会静默回退成单行 SPECIAL_TOTAL、丢失 3 类明细。已加入候选并置顶 + 布尔解析改稳健版（防 `"False"` 被 `astype(bool)` 误判）

### 核心发现
1. **`nonzero_od_cells` 恒为 71,838 = 234×307，与 λ 无关**（有效支撑 = P>0 的 234 origin × A>0 的 307 dest；引力种子在支撑上恒正，IPF 乘法缩放不制造/消灭零）→ **`matrix_density` 不是有效的 λ 诊断量**；有意义的稀疏度要按阈值（cells ≥1/≥10/≥100）或集中度（top1% 份额）看
2. **⚠️ 平均通勤 11–14 min 偏低（现实汽车早高峰 ~25–30 min）**，根因是 **Step 4 是 free-flow 阻抗**（median 14.88 min、0% 超 90 km/h）→ **λ 无法修复，需引入早高峰速度折减或 LTA 实测速度**。>45 min 出行占比 ≤0.02%（几乎为零），>60 min ≈ 0
3. **✅ 空间结构强正信号**：Region 级 Central P 21.7% / **A 53.7%（A/P=2.17 净输入）**；NORTH-EAST P 23.3% / A 8.4%（A/P=0.32 卧城）——与新加坡真实结构一致
4. 最大产生区 TAMPINES EAST 73,517（3.33%）；最大吸引区 **CHANGI AIRPORT 26,898（1.39%）居首**，其后 CBD（CECIL/RAFFLES PLACE/CITY HALL 各约 1.3%）
5. **⚠️ 无路网 5 区 P=0 但仍有 A=4,506（0.2329%）**：SEMAKAU 1,672 / SUDONG 1,384 / SOUTHERN GROUP 1,450（到发时间靠 1.8–6.3 km 几何外推）→ Step 5B 建议将其 A 置 0
6. **⚠️ `c_ii=0` 使自环被过度加权**：区内份额随 λ 从 0.63%→1.78%；λ=0.10 下第 2 大流即 TAMPINES EAST→TAMPINES EAST（2,140）→ 建议给对角一个小区内阻抗下限
7. 记账残差：`phys + special = 1,935,235 + 273,124 = 2,208,359` vs `P_total 2,208,356` → **+3**（源表取整，与 Step 2/3 同源）
- 明确未做：Census Table 118 约束、Car-only OD、MATSim assignment、TrafficFlow 校准

## Step 5B Census Table 118 + c_ii 修正：build_prior_od_5b.py（PASS，约 1s）

- 用户提供 `scripts/od/build_prior_od_5b.py`；输出 `reports/od_prior_5b/`（**不覆盖 5A**）：attraction_carbon.csv、intrazonal_impedance.csv、table118_region_pa_constraints.csv、`prior_od_5b_lambda_{0p050..0p150}.parquet/.csv`、region_pa_validation_lambda_*.csv、prior_od_5b_summary.csv、prior_od_5b_accounting.json，另加 **STEP5B_REPORT.md**
- 用户确定流程链：**5A Free-flow Gravity ✅ → 5B Census+cii → 5C AM Peak 阻抗 → 6 MATSim → 7 TrafficFlow 校准**

### 我修复的 4 处（均非模型口径）
1. pandas 3.0 `stack(dropna=False)` 已废弃 → 改 `stack()`（矩阵完整）
2. **`load_t11` 区域列前缀大小写**：T11 实际列是 `Central Region_Mode of Transport_...`（标题式），脚本用全大写 `CENTRAL REGION_` 前缀 → 命中 0 列、Table 118 会解析为空。改为忽略大小写匹配（内部键仍大写）
3. pandas 3.0 `to_numpy()` 返回**只读**数组，`pt *= 标量` 抛 `output array is read-only` → 改非原地赋值
4. **`clean_attraction` 公式错误**：原用只含 3 行的 `special_workplace_destinations.csv` 的 `workplace_employment` 当权重 → 全部真实 PA 权重为 0、吸引量被整体清零（表现为 Physical production=0、mean_tt=nan）。按用户公式重写 `A'_j = E_PA·A_j/Σ_{k∈PA,k∉R}A_k`，**E_PA = 该 PA 原始物理吸引总量**（attraction_v21 已逐 PA=census_control，无需外部 PA 控制表）。（注：该处首次编辑未落盘，重新应用并核验）

### 修正 A：无路网 Zone（5 个）
- 260 NESZ01/NORTH-EASTERN ISLANDS 原 A=0；258 SISZ02/SOUTHERN ISLANDS 1450.41；271 SMSZ04/SIMPANG 原 A=0；322 WISZ02 + 323 WISZ03 /WESTERN ISLANDS 1671.69+1384.38
- 移除合计 **4,506.484**（0.23%），**同 PA 内重归一化**（不删总量）→ 全局 ΣA 1935235→1935235（rel 1e-16），受影响 PA（SOUTHERN ISLANDS、WESTERN ISLANDS）总量守恒 1e-16

### 修正 B：c_ii 代理值（0.5×最近5个其它Subzone中位阻抗）
min 0.313 / median **1.594** / mean 1.866 / max 13.438 min（邻域中位 3.188）。**代理值偏小**（自由流下最近5区仅相隔3.19 min，刻画的是"到邻区边缘"而非完整区内出行）→ 5C 应重估

### 修正 C：Table 118 → ResidenceRegion×WorkplacePA（44 PA×5 Region=220 单元，四方式合并）
- 约束贴合（λ=0.10）：**mean rel_err 0.57%、max 2.29%、>1% 38 单元、>5% 0 单元**
- **2.2% 是数据内在不一致的硬地板**：块约束↔边际交替迭代 0次=205.76% → 1次=2.29% → 2次后**恒为2.208%不再下降**；直接检验 PA 级"Census隐含质量 vs 吸引量边际"最大不一致 **2.06%**、均值 0.55%、6 个 PA>1% → 块约束与行列边际**无法同时精确满足**，单次实现已最优，**无需迭代返工**（绝对质量冲突仅 ~480/1935235=0.025%）

### 5 套 λ 结果（全收敛）
- od_total≡1,935,235；row/col err ≤6.4e-9；nonzero 恒 **71,136**（=5A 71,838 − 5 无路网区支撑的 702 格）
- **平均通勤：12.344 / 12.218 / 12.089 / 11.958 / 11.825 min（λ 0.05→0.15）**，λ 敏感度变平（5A 跨度 2.48 min → 5B 0.52 min）
- 5A 对照：13.740/13.073/12.425/11.816/11.259

### 消融（λ=0.10，平均通勤）
- c_ii=0→1.594：**+0.034 min**（区内份额仅 1.1→1.2%，λ对1.6min惩罚很轻）→ **c_ii 意义在局部结构不是全局均值**
- 无路网清理：−0.007 min
- **Table 118 约束：−0.363 min（5B 均值下降的主因）**：纯 Gravity+自由流会把出行铺太远（无约束块误差205.8%），Census 把目的地拉回更近 PA

### Region→PA 结构（λ=0.10）与 Region A/P
- 5 区总产生：CENTRAL 419,823 / EAST 328,892 / NORTH 282,070 / NORTH-EAST 450,760 / WEST 453,691
- **DOWNTOWN CORE 在全部 5 区均为 Top-1**（10.0%–21.6%，CBD 集聚明确）
- Region A/P：**CENTRAL 2.47（居住21.7%/岗位53.7%）**、EAST 0.74、NORTH 0.52、**NORTH-EAST 0.36（卧城）**、WEST 0.76（5A 记的"2.17"系笔误，份额比应为 2.47）

### 命名修正（用户§6）
`physical_workplace_share` → **`physical_workplace_mass_share`**（=0.876324），accounting 内显式保留定义（**不是**"87.63%居民去实体workplace"，仅为对齐332×332物理OD与Census PA就业吸引的**质量分配系数**）与假设（缺Subzone级Other/NoFixed/WFH居住分布→按居住产生量比例分摊，**假设保留**）

### 下一步
Step 5B **PASS、无需返工**。进 **Step 5C（AM Peak 阻抗）**：① 优先查 `TrafficSpeedBands_v4.json`/`EstimatedTravelTimes.json` 是否含 Link 级时段速度/旅行时间，**不要**用 `TrafficFlow_Data.json` 的 Volume 反推速度；② 重估 c_ii；③ 同一约束框架（无路网清理+Table118+IPF）替换阻抗。λ 未冻结，λ* 交 Step 7 TrafficFlow 校准。

## Step 5C AM Peak 阻抗：build_am_peak_impedance.py（PASS，2m12s）

- 脚本 `scripts/od/build_am_peak_impedance.py` → `reports/od_impedance_ampeak/`；5C 版 Prior OD → `reports/od_prior_5c/`；另加 **STEP5C_REPORT.md**

### 数据实况（先勘查再跑，避免了 2 个方向性错误）
- **`TrafficSpeedBands_v4.json` 是 2026-05-05 18:53（周二傍晚）单次快照**，不是 AM 历史聚合 → 用它按 07–10 过滤会得 0 条
- 带时间戳文件全在 **`realtime_monitoring/`**（144 个 / **7.4 GB**），**工作日 07:00–09:59 只有 12 个**（20260506 周三 6 个 + 20260507 周四 6 个，各 07:00/07:30/08:00/08:30/09:00/09:30），每个 **143,787 条 = 全 Link 覆盖**
- 快照 JSON **无 Timestamp 字段**，时间只能由文件名解析；**必须按文件名预筛**否则要解析 7.4 GB
- **正确几何源 = `historical_data/TrafficSpeedBands_Links.shp`（143,787 条，与 JSON 一一对应）**；`08_TrafficCount/TrafficFlow_Links.shp` 只有 **1,278 条**（交通量断面），用它匹配率上限 0.9%

### 修复的 5 处（3 阻塞 + 1 致命数据 bug + 1 性能）
1. **缺 `network_nodes.csv`**：Step 4 只存边未存节点坐标 → 给 `build_impedance.py` 加节点表导出（`node_id,x_svy21_m,y_svy21_m,lon,lat`，429,032 行）→ 重跑 **1m25s**，Step 4 指标**逐位一致**（reachable 1.0 / median 14.878 / max 64.066，确定性已验证）；旧产物备份在 `reports/od_impedance/_pre_5c_backup/`
2. **LTA_LINKS 指错文件**：TrafficFlow_Links.shp(1,278) → TrafficSpeedBands_Links.shp(143,787)
3. **SPEED_DIR 指错目录**：只扫 historical_data → 加 realtime_monitoring + 文件名预筛
4. **⚠️⚠️ 致命：Band 8 的 `MaximumSpeed=999` 是哨兵值不是 999 km/h**。原护栏 `if vmax>=1000` → 999 漏过 → `(70+999)/2=534.5 km/h` 赋给所有高速（PIE/AYE/ECP/TPE/CTE/SLE/BKE/KJE/MCE 等 2,016 条 Link）→ 高速近乎免费、严重低估 AM 拥堵。修法：`vmax>=999` 或 band==8 一律回退可配置代理值 **75 km/h**。影响：AM 最大速 534.5→90 km/h、>130km/h 边 1,790→**0**、OD 中位 16.77→**18.13 min**、变慢 OD 对 92.05%→**99.67%**。v1 输出留档 `reports/od_impedance_ampeak/_v1_band8_sentinel_bug/`
5. **性能**：原实现每条记录 `pd.to_numeric(pd.Series([x]))`（≈700 万次 Series）+ 匹配 14 万×8 次 pandas `.iloc`/shapely 标量 → **>17min 卡在 Step 1**。改：标量 `tofloat` + 每文件解析一次列名；LTA→OSM 匹配**整体向量化**（KDTree + 广播端点误差 + argmin）→ **2m12s**
- 附：snap 表列名 `nearest_node`（脚本要 `nearest_network_node`）→ 兼容两种

### 结果
- **LTA→OSM 匹配率 99.82%（143,478/143,735 Link）**；**92,149 条 OSM 边获得 AM 速度 = 13.0% 边数 / 18.1% 长度**（未匹配保留 free-flow）→ **覆盖率是主要局限，×1.222 属下界**
- 各档 AM/FF 中位速比：residential 50→19.5(0.39)、primary 60→44.5(0.74)、secondary 50→34.5(0.69)、tertiary 50→29.5(0.59)、trunk 70→44.5(0.64)、**motorway 90→64.5(0.72)**；匹配边 89.6% 变慢；长度加权 AM 速 19.9 km/h；按档中值加权全网 AM 均速 ≈25.5 km/h（与新加坡早高峰量级一致）
- **OD 层面：中位 14.878 → 18.131 min（+3.18，×1.222）；均值 15.197→18.481；最大 64.066→78.953**；332×332 全可达 → **"明显高于 14.88"：是**
- **5C Prior OD（同一约束框架+AM 阻抗，5 λ）**：平均通勤 **15.312/15.119/14.922/14.720/14.514 min**（λ 0.05→0.15）；对 5B 比值 1.227–1.240；od_total≡1,935,235；row/col ≤6.4e-9；5/5 收敛；非零格恒 71,136
- **c_ii 随阻抗重估**：FF 中位 1.594 → **AM 中位 2.079 min**
- ⚠️ Region→PA 残差随 λ 升高（2.3%→**4.01%@λ=0.15**）：衰减越强越难兼顾 Census 区块与吸引量边际；λ≤0.10 仍 ≤2.4%（接近 2.2% 地板）

### 给 `build_prior_od_5b.py` 加的 CLI（复用同一约束框架）
`--out / --prefix / --impedance / --label`；5C 用 `--out reports/od_prior_5c --prefix prior_od_5c --impedance reports/od_impedance_ampeak/ampeak_impedance_matrix.parquet`

### 下一步（待用户决策）
Step 5C **PASS**。**先确认匹配覆盖率是否接受**（13% 边/18.1% 长度）：接受 → 进 **Step 6 MATSim**；要提高 → 先做 **5C.2**（按**路名**传播，需 Step 4 在 network_links.csv 增存 road name；或放宽 80m 半径 `--max-match-distance`），再重跑 5C 的 Gravity/IPF。λ* 仍交 **Step 7 TrafficFlow** 校准。

---

## Step 5C.1：AM 速度覆盖增强（三级传播）— PASS（本轮，45s）

用户决定：**不采用简单放宽半径**（已有 99.82% LTA Link 匹配，放宽只会引入错配），改为做**同名道路传播**；并冻结 ① 3B.1 Attraction ② 5B Census-constrained Prior OD 基线；**暂不冻结 5C final λ**。新建 `scripts/od/enhance_ampeak_network.py`，输出 `reports/od_impedance_ampeak_v2/`（**不覆盖** v1 与 5C）。

### 运行前修掉的 4 处（2 阻塞 + 1 结构性缺口 + 1 性能）
1. **SyntaxError**：`main()` 里 `global BAND8_SPEED,GEOM_MAX_M` 声明晚于其作为 `add_argument(default=...)` 的使用 → 脚本从未跑起来。修：global 并入 main() 首行（同类坑：`NAME_MIN_LTA_LINKS` 同因，一并前置）。
2. **`LTA_LINKS` 指向不存在的 `08_TrafficCount/TrafficSpeedBands_Links.shp`**；真实位置 `Dynamic_2026_03_16/historical_data/`。修：候选路径列表自动择优。
3. **⚠️ 结构性缺口：Step 4 从未读取 OSM `name`** → `network_links.csv` 只有 6 列无 `name` → 5C.1 的 `name_norm` 恒空、**同名传播实际完全不生效**（脚本会“跑通”但等于纯几何）。修：`build_impedance.py` 的 `READ_COLS` 加 `name`；`build_graph` 贯通 `name`；`network_links.csv` 扩为 `…,highway,lanes,name`（`name` 覆盖 34.4% 边 / **41.1% 长度**；`lanes` 59.8%）。Step 4 重跑 **1m13s**，`C^FF` 指标**逐位一致**（reachable 1.0 / median 14.877969777832641 / max 64.06551699576448）。
4. **性能**：逐记录 key 扫描（≈700 万次）+ 逐行 `.iloc` 会 >15min → 改定长字段 + numpy 数组访问 → **45s**。

> ⚙️ 工具坑：对**同一文件并行**发起多个 Edit 会读-改-写竞争、静默丢改动（6 处改造先只落 4 处）→ 必须**串行**编辑。

### 三级传播设计（逐边记录 `assignment_tier`）
`geom`（LTA Link 质心→最近 OSM 边 ≤100m，路名一致优先，最高置信度）> `name+hw`（同名 且 highway 组与该路名已命中边一致）> `name`（仅同名）> `none`（保留 FF）。
- 路名归一化：大写 + 标点/连字符→空格 + 后缀缩写（AVENUE→AVE…），两侧一致；实测 **95.7% LTA 路名可在 OSM 命中**（4,040/4,221）。
- 路名需 ≥`--name-min-links`(=3) 条 LTA 证据才用于传播；软护栏 AM ≤ FF×1.5 且 ≥5 km/h（触发 1,800 边）。
- Band8 `MaximumSpeed=999` 哨兵显式拦截 → 75 km/h。

### 结果（`ampeak_v2_validation.json`）
- **AM 覆盖：18.1% → 38.35% 长度**（边 13.0% → 32.17%）；tier：geom 18.65% + name+hw 19.66% + name 0.04%，none 61.65%。
- 几何匹配 LTA Link 143,562（v1 143,478）；几何匹配 OSM 边 87,195（v1 92,149）；路名命中率 91.93%。
- **三层阻抗：`C^FF` 14.878 / `C^AM,v1` 18.131 / `C^AM,v2` 21.361 min**；均值 15.197/18.481/21.724；最大 64.066/78.953/89.301。AMv2/FF=1.461，AMv2/AMv1=1.185；**100% 单元 AMv2≥AMv1**（95.5% 涨>1min，55.6% 涨>3min）。
- 各道路组 AM/FF 中位比（覆盖边）：**expressway 0.78 / arterial 0.58 / local 0.49**；`>130km/h 边=0`；可达率 1.0。
- 覆盖率天花板：OSM 机动车边有名字者占 41.1% 长度、其中名命中 LTA 占 **37.4% 长度** → 5C.1 的 38.35% 已基本触顶，剩余 none 多为**无名 local/service**。

### 交付
- 新建 `scripts/od/readme.md`（总流程设计 + 脚本清单/代码设计 + 运行状态 + 坑与约定 + 复用命令 + 冻结/待定 + 变更记录）。
- 新建 `reports/od_impedance_ampeak_v2/STEP5C1_REPORT.md`。
- **未重跑 5C Gravity**（按用户约定）。下一步：接 5B 约束框架生成 5C.1 Prior OD：
  `python scripts/od/build_prior_od_5b.py --out reports/od_prior_5c1 --prefix prior_od_5c1 --impedance reports/od_impedance_ampeak_v2/ampeak_impedance_v2.parquet --label "Step 5C.1"`（粗估均值≈17.7min，需实跑确认）。λ 仍不冻结，交 Step 7。

---

## Step 5C.1 Prior OD：AM-v2 阻抗 + 5B 约束框架 — PASS

用户决策：**接受 38.35% AM 覆盖，不再追覆盖**（剩余 61.65% 无名 local/service 无可靠依据，扩大半径会致系统性错配）；直接进 Step 5C.1 Prior OD。λ 仍不冻结，交 Step 6 MATSim → Step 7 TrafficFlow。

### 运行前修掉脚本 2 处致命缺陷（用户提供的 `build_prior_od_5c1.py` 本不可运行）
1. **SyntaxError**：文件尾部残留代码生成器包装（第 192–195 行 `'''` + `Path('/mnt/data/...').write_text(code)`）→ 删除。**教训**：用户"已整理好"的脚本仍须先 `py_compile` 验证。
2. **NameError**：`accounting` 里引用了未定义常量 `AMPEAK_IMPEDANCE` → 改为已定义的 `IMPEDANCE`。

### 产物 `reports/od_prior_5c1/`（不覆盖任何既有目录）
`prior_od_5c1_lambda_0p{050,075,100,125,150}.parquet/.csv`、`prior_od_5c1_summary.csv`、`prior_od_5c1_accounting.json`、`attraction_clean_5c1.csv`、`intrazonal_impedance_5c1.csv`、`table118_region_pa_constraints_5c1.csv`、`region_pa_validation_lambda_*.csv`；另新建 `STEP5C1_PRIOR_OD_REPORT.md`。

### 守恒（验收①②）
5 组 λ **全部收敛**；`od_total=1,935,235`（精确）；行边际最大误差 **≤9.65e-9**、列 **≤3.27e-11**；非零格恒 71,136；`mass_share=0.876324`（与 5B 同）；无道路 Zone 5 个，移除 A=4,506.48。

### Region×PA 贴合（验收③）— ⚠️ 随 λ 上升，与 5B 行为不同
| λ | 5C.1 max rel | 5B（对照） | max abs diff |
|---|---|---|---|
| 0.050 | **2.32%** | 2.28% | 192 |
| 0.075 | 2.25% | 2.30% | 252 |
| 0.100 | 2.49% | 2.29% | 543 |
| 0.125 | 3.67% | 2.42% | 786 |
| 0.150 | **5.76%** | 2.73% | 1,148 |
机制：AM-v2 更重罚跨区出行，`exp(−λC)` 在大 λ 下强烈对抗 Census 跨区结构 → 块约束与边际张力放大，末次 IPF 回拉边际时块偏差增大。残差集中少数块（低 λ：PUNGGOL；高 λ：BUKIT PANJANG/BOON LAY/PASIR RIS），绝对量 ≤1,148/1,935,235≈0.06%（2.2% 硬地板之上）。**含义：进 MATSim 建议先用 λ=0.05–0.10。**

### T^5B vs T^5C.1 对比（验收④）— 关键反直觉发现
- **加权均值时长（各自口径，min）**：5B 12.344→11.825；5C 15.312→14.514；**5C.1 18.082→16.960**（λ 0.05→0.15）。
- **换 AM-v2 后 OD 反而略偏本地化**：区内份额系统性升高（λ=0.10：1.15%→**1.47%**，+27%），跨区份额/`>30min` 长距离份额微降 → **同一口径（@AM）下 5C.1 平均时长比 5B 低 0.2–0.6min**。阻抗层变长 ≠ Prior OD 平均时长变长。
- **Census Region×PA 结构对阻抗几乎不敏感**（被强加）：块份额 L1 变化 0.10%(λ0.05)→0.46%(λ0.10)→2.21%(λ0.15)；单块最大 Δ≤0.23pp。
- **λ 的影响 > 阻抗的影响**：5B↔5C.1 总变差距离 2.3%→6.7%（λ 从小到大）。
- 新建只读脚本 `scripts/od/compare_prior_od.py`（5B vs 5C.1 对比 + 守恒校验）。

### 交付与下一步
- 更新 `scripts/od/readme.md`：流程图加 5C.1 Prior OD；脚本清单加 5C.1b/compare；状态表、λ 敏感性表（加 5C.1 列）、冻结表、变更记录全部同步。
- **λ 未冻结**。下一步：**Step 6 MATSim assignment**（建议 λ=0.05/0.075/0.10 三档起）→ `q_a^sim` → Step 7 对 TrafficFlow `q_a^obs` 校准定 λ*。
- 坑复现：pandas 3.0 `to_numpy()` 只读 → `m[pos,pos]=...` 报 `assignment destination is read-only`，须先 `.copy()`。

---

## Step 5C.1 收口 + Step 6.1 MATSim 网络底座 — PASS

用户确认：**Step 5C.1 Prior OD PASS**，作为进 MATSim 前的候选先验 OD；**λ 仍不冻结**。
冻结关系：3B.1 Attraction ✅ / Step 4 FF 阻抗 ✅ / 5B 基线 ✅ / 5C(AM-v1) 对照 ✅ / 5C.1(AM-v2) 当前候选 ✅；λ∈{0.05,0.075,0.10,0.125,0.15}；Step 6→下一步，Step 7 定 λ*。
关键结论（用户拍板）：5C.1 Region×PA 偏差随 λ 增大**不是模型坏了**，而是 **Census 结构约束 与 e^(−λC^AM) 的竞争加剧**；故 λ=0.10 只能作 MATSim **中间试验值**，Step 6 先跑 **0.05/0.075/0.10** 三档（对 Table 118 更稳且覆盖足够宽的距离衰减）。
口径重申：`T^5C.1` 是**物理 Workplace OD，Σ=1,935,235 ≠ 全部就业居民 2,208,358**；其余 ~27.3 万属 Other Planning Areas/Outside SG、No Fixed Location、Works from Home，**永远作特殊目的地质量保留，不塞进 332 个物理 Subzone**（保持到 MATSim 设计阶段）。

### Step 6 规划（用户定义）
Prior OD → MATSim population/plans → MATSim assignment → q_a^sim → 对 TrafficFlow q_a^obs 校准定 λ*。
⚠️ 第 3 项要点：**不能 1 个 OD cell = 1 个 agent**，须按 Production 总量生成 agent 数并**保存 expansion factor**，否则后续 TrafficFlow 比对无法解释。

### Step 6.1 结果（`scripts/od/build_matsim_network.py`，纯网络底座转换）
运行前先验证：脚本语法 OK（本次用户脚本**无**上次那种残留缺陷）。逐步校验输入：network_links 706,554 边 / 0 重复有向对；network_nodes 429,032 / id 唯一 / 端点全有效；zone_dictionary 含 subzone_name；snap 有 nearest_node；0 自环 / 0 孤立节点 / speed 5–90 无零无 NaN / length 无零。
产物 `reports/matsim_network/`：`network.xml.gz`(13.5MB)、`network_links_source_copy.csv`、`network_nodes_source_copy.csv`、`zone_matsim_node_map.csv`(332)、`network_validation.json`。
`network_validation.json` 关键：status PASS；nodes 429032；directed_edges 706554；zone_count 332；duplicate_directed_edges 0；speed_median 20 / max 90 km/h；lanes_median 1；zone_snap_max_m 6339.46；`capacity_assumption = lanes × highway-class default veh/h/lane`（**假设值，非观测容量**，参数已落盘，Step 7 单独校正，不与 OD 校准混）。

### 我额外做的独立复核（新建 `validate_matsim_network.py`，不信任自报告 JSON）
重解析 `network.xml.gz`：xml_nodes 429032 / xml_links 706554 / 0 重复 node id / 0 重复 link id / 0 坏引用 / 0 自环 / 0 缺属性；freespeed 5–90 km/h；capacity 400–13300；permlanes 1–8；zone_map 332 行 0 坏引用；**ALL_CHECKS_PASS=true**。
另查明：吸附 >1000m 的正好是 **5 个无道路离岛**（NORTH-EASTERN ISLANDS/SOUTHERN GROUP/PULAU SELETAR/SEMAKAU/SUDONG，最大 6.34km）——它们 **P=0 且 A=0**（生产端 work_trip_production 全 0，吸引端 5B/5C.1 已置 0），**不产生/不吸引出行，该吸附值无实际影响**。中位吸附 26m / p90 154m。

### ⚠️ 性能与 gzip 坑（重要）
- 原脚本对 706,554 边做 `pd.Series(r._asdict())` 逐行构造（×2/边）→ **9m00s**。已向量化 `lanes_vec`/`capacity_vec` → **38.5s（14×）**；用固定 gzip mtime 复核证明 **XML 内容逐字节一致**（lanes/capacity max|diff|=0，节点序一致，`sha256(uncompressed)=7b81b8e8515c0b32d6e0c1dda746501077ae85560ab9844bee085fca224f52aa`），磁盘文件与之一致。临时验证脚本已删。
- **gzip 头内嵌 MTIME=写入时间** → `network.xml.gz` 的文件哈希每次运行都变，**不能拿 .gz 哈希做内容比对**；要比就比**解压后**字节。

### 交付与下一步
- 更新 `scripts/od/readme.md`：流程图加 6.1/6.2/6.3/7；新增 §2.4 Step 6.1 设计要点（含 capacity 假设、性能、gzip 坑）；脚本清单加 6.1/validate；状态表加 6.1–6.3；复用命令加 build/validate；冻结表；变更记录。
- **下一步 Step 6.2：Prior OD → MATSim Population/Plans**（须存 expansion factor，不可 1 cell=1 agent）。
